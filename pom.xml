<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>gg.rc</groupId>
  <artifactId>rcparties-parent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>RCParties</name>
  <description>Game-agnostic player grouping primitive (RC-SPEC-PARTIES-001)</description>

  <modules>
    <module>rcparties-api</module>
    <module>rcparties-plugin</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>
    <paper.version>26.2.build.121-stable</paper.version>
    <rcui.version>3.0.0</rcui.version>
    <!-- Escape hatch for anyone resolving RCUI/RCPlatform from an internal repository. -->
    <rcparties.skipDependencyCheck>false</rcparties.skipDependencyCheck>
    <rcplatform.version>1.0.0</rcplatform.version>
    <junit.version>5.11.3</junit.version>
  </properties>

  <repositories>
    <repository>
      <id>papermc</id>
      <url>https://repo.papermc.io/repository/maven-public/</url>
    </repository>
  </repositories>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>io.papermc.paper</groupId>
        <artifactId>paper-api</artifactId>
        <version>${paper.version}</version>
        <scope>provided</scope>
      </dependency>
      <!-- RCUI ships its API inside its own plugin jar, and RCUI needs RCPlatform.
           Both MUST stay 'provided': a second copy on the server is a LinkageError. -->
      <dependency>
        <groupId>net.republicraft</groupId>
        <artifactId>RCUI</artifactId>
        <version>${rcui.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>net.republicraft.platform</groupId>
        <artifactId>rcplatform-api</artifactId>
        <version>${rcplatform.version}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>enforce-rc-dependencies</id>
            <phase>validate</phase>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <skip>${rcparties.skipDependencyCheck}</skip>
              <rules>
                <!-- RCUI and RCPlatform are not on any public Maven repository, so a
                     missing one surfaces as an opaque "Could not find artifact" dump
                     naming repositories that were never going to have it. Check the
                     local repository first and say what to actually do about it. -->
                <requireFilesExist>
                  <files>
                    <file>${settings.localRepository}/net/republicraft/platform/rcplatform-api/${rcplatform.version}/rcplatform-api-${rcplatform.version}.jar</file>
                    <file>${settings.localRepository}/net/republicraft/RCUI/${rcui.version}/RCUI-${rcui.version}.jar</file>
                  </files>
                  <message>RCParties needs RCUI ${rcui.version} and RCPlatform ${rcplatform.version} in your local Maven repository. Neither is published to a public repository, so build them yourself IN THIS ORDER (RCUI compiles against rcplatform-api):

  git clone https://github.com/CoffeePNG/RCPlatform
  cd RCPlatform
  mvn -B install -DskipTests
  cd ..

  git clone https://github.com/CoffeePNG/RCUI
  cd RCUI
  mvn -B install -DskipTests
  cd ..

Both require JDK 25, the same as RCParties. If you resolve these from an internal repository instead, skip this check with -Drcparties.skipDependencyCheck=true</message>
                </requireFilesExist>
              </rules>
            </configuration>
          </execution>
          <execution>
            <id>enforce-jdk</id>
            <phase>validate</phase>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion>
                  <!-- Fail with a message that names the JDK, rather than javac's
                       bare "release version N not supported". -->
                  <version>[25,)</version>
                  <message>RCParties targets Purpur/Paper 26.2 and requires JDK 25 or newer. Point JAVA_HOME at a JDK 25+ install, or build the legacy/1.21.11 branch, which targets Paper 1.21.11 on JDK 21.</message>
                </requireJavaVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>3.6.0</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
